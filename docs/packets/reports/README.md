# Packet run reports

One file per completed packet run: `packet_NNN_report_YYYYMMDD.md`, committed by the
running session in its close-out PR alongside the INDEX status flip. Reports are
append-only history — never edited after landing.
